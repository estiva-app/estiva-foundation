# @estiva-app/hello

## 0.0.2

- Adds `farewell`, so an upgrade is observable in the type surface as well as at
  runtime.
- `HELLO_VERSION` → `0.0.2`.
- First release published by CI on a tag, through trusted publishing. `0.0.1`
  was published by hand, because a trusted publisher can only be configured on a
  package that already exists.

## 0.0.1

- First publish. `hello`, `greet`, the `Greeting` type.
